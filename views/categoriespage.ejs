<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <% if(genres.length>=1){ %>
        <% for(genre of genres){ %>
            <a href="/categories/<%=genre %>"<><%=genre %></p>
        <%} %>
   <% } %>
   <a href="/">Go back</a>
  </body>
</html>
